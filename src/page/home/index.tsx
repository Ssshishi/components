import React, { useEffect } from 'react'
import { NavBar } from 'antd-mobile'

export const Index: React.FC = () => {
  useEffect(() => {
    return () => {
    }
  }, [])

  return (
    <div>
      <NavBar>首页</NavBar>
    </div>
  )
  
};